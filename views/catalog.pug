extends layout 

block content 
  h2= title
  div(class='items')
    each item in items 
      div(class='item')
        a(href=item.url) #{item.name} 
        - var img = item.img !== '' ? '/uploads/' + item.img : '/uploads/no-pic.webp'
        img(src=img)
        p Category: #{item.category}
        p Price: #{item.price}£
        p stock: #{item.stock} 
        a(href=item.url)
          button See details
