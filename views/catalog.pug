extends layout 

block content 
  h2= title
  div(class='items')
    each item in items 
      div(class='item')
        a(href=item.url) #{item.name} 
        p #{item.category}
        p #{item.price_formatted}£
        p stock: #{item.stock} 
        a(href=item.url)
          button see details
